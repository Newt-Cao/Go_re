作业一：
    1.创建一个数组模拟队列，每隔一定时间（随机），给该数组添加一个数。
    2.启动两个协程，每隔一定时间（时间随机），到队列取出数据。
    3.在控制台输出：
        x 号协程服务——> x 号客户
        x 号协程服务——> x 号客户
    4.使用锁机制。

作业二：
    1.创建一个链表模拟队列，实现数据入列、出列、显示队列。

作业三：
    1.理解快速排序和插入排序法。